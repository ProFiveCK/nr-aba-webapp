# Frontend (React + Vite)

The production build output is written to `dist/` and is served by the Nginx `web` container (mounted at `/usr/share/nginx/html`).

## Commands

```bash
npm install

# Dev server (optional)
npm run dev

# Production build
npm run build
```
