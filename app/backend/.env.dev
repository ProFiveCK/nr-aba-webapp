# Server
PORT=4000

# Database (using same as production for testing)
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=pG7!xP@2vQz#1sL9w
DB_NAME=aba

# Authentication
JWT_SECRET=dev_jwt_secret_for_local_development_only
REVIEWER_SESSION_MINUTES=480
REVIEWER_TEMP_PASSWORD_LENGTH=12
BCRYPT_ROUNDS=10  # Lower for faster development

# Front-end URL for email links and login redirects (local development)
FRONTEND_BASE_URL=http://localhost:9001

# SMTP relay (disabled for development - you can enable if needed)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=dev@localhost
# REPLY_TO_EMAIL=dev@localhost

# SFTP Sync Configuration (disabled for development)
SFTP_SYNC_METHOD=disabled
# WINDOWS_SYNC_URL=
SYNC_TIMEOUT=30000

# Development specific
NODE_ENV=development